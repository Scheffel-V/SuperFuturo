# SuperFuturo Application
## An intelligent supermarket Application.

### Backend
The backend REST API was developed using **Java**, with **Spring Framework**, **Spring Boot** and **Hibernate**. The database uses **H2**.
The API documentation can be found here:
SuperFuturo Application API (to be developed)

### Frontend
The frontend was developed using (to be decided).

### Tests
There is (NUMBER OF TESTS) tests, used to test the basic functionalities of the REST API.
To run the tests, you will need to install Java and Maven. After that, run the following commands inside the root directory of the repository:
```
cd backend
mvn test
```

### Comments


### Todo
It is still possible to improve the system, ...

### Run
#### Backend
To run the backend, you will need to install Java and Maven. After that, run the following commands inside the root directory of the repository:
```
cd backend
mvn spring-boot:run
```
The backend server will be running on: 
```
http://127.0.0.1:8080/
```

#### Frontend
To run the frontend, you will need to install (to be decided). Then, run the following commands inside the root directory of the repository:
```
cd frontend
(to be decided)
```
The frontend server will be running on: 
```
http://127.0.0.1:4200/
```
